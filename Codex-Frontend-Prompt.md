# Codex Prompt — Venderly Swim Club v2 React Frontend

Paste the entire content below into Codex as a single prompt. Stay in the working v2 backend repo (the one with the Postgres backend + `/api/v1/...` endpoints). The frontend lives in a new `frontend/` subdirectory and is served by the same Express server as static files.

**Companion docs (already in the repo):**
- `Design-Tokens.md` — source of truth for all colors, spacing, typography (paste relevant sections into your build)
- `V2-Architecture.md` — overall system architecture

---

## PROMPT FOR CODEX

```
Build a production-grade React frontend for Venderly Swim Club v2 — a 
staff dashboard used on iPads at pool entrances by teenage lifeguards 
and viewed on phones by managers.

═══════════════════════════════════════════════════════════════
PROJECT SETUP
═══════════════════════════════════════════════════════════════

Location: frontend/ subdirectory inside the existing project root
Tech stack:
  - Vite 5 + React 18 + TypeScript (strict mode)
  - Tailwind CSS 3.4
  - shadcn/ui (latest)
  - TanStack Query v5 (server state)
  - TanStack Router (client routing — type-safe, code-split)
  - Zustand (lightweight UI state, e.g. theme)
  - react-hook-form + Zod (forms + validation)
  - lucide-react (icons)
  - sonner (toast notifications)
  - date-fns (date formatting)
  - clsx + tailwind-merge (className utilities — via cn() helper)
  - Vitest + @testing-library/react (tests)
  - Playwright (E2E)

Init commands:
  cd frontend
  pnpm create vite . --template react-ts
  pnpm install
  pnpm install tailwindcss postcss autoprefixer @tailwindcss/forms
  pnpm install @tanstack/react-query @tanstack/react-router
  pnpm install zustand react-hook-form zod @hookform/resolvers
  pnpm install lucide-react sonner date-fns clsx tailwind-merge
  pnpm install -D @types/node vitest @testing-library/react @testing-library/jest-dom
  pnpm install -D playwright @playwright/test
  pnpm dlx tailwindcss init -p
  pnpm dlx shadcn@latest init

Configure shadcn with:
  - TypeScript: yes
  - Style: Default
  - Base color: Slate (we override with our own tokens)
  - CSS variables: yes
  - tailwind.config.ts location: tailwind.config.ts
  - components.json paths: src/components/ui

═══════════════════════════════════════════════════════════════
FILE STRUCTURE
═══════════════════════════════════════════════════════════════

frontend/
├── index.html
├── tailwind.config.ts          ← from Design-Tokens.md
├── postcss.config.js
├── vite.config.ts              ← proxy /api/* to backend:3000
├── tsconfig.json
├── components.json             ← shadcn config
├── playwright.config.ts
├── vitest.config.ts
├── src/
│   ├── main.tsx
│   ├── App.tsx                 ← TanStack Router setup
│   ├── routeTree.gen.ts        ← generated by TanStack Router
│   ├── styles/
│   │   ├── globals.css         ← from Design-Tokens.md (CSS vars + light/dark)
│   │   └── fonts.css           ← Inter font import
│   ├── lib/
│   │   ├── api.ts              ← Axios + interceptors + auth header
│   │   ├── auth.ts             ← in-memory JWT, no localStorage
│   │   ├── queryClient.ts      ← TanStack Query config (3s polling)
│   │   ├── sse.ts              ← Server-Sent Events hook
│   │   ├── theme.ts            ← light/dark/system theme hook (no localStorage)
│   │   ├── format.ts           ← formatPhone, formatTime, formatDuration
│   │   ├── cn.ts               ← classNames helper
│   │   └── types.ts            ← shared API types
│   ├── components/
│   │   ├── ui/                 ← shadcn primitives (button, card, input, etc.)
│   │   ├── atoms/
│   │   │   ├── Logo.tsx
│   │   │   ├── ConnectionIndicator.tsx
│   │   │   ├── ThemeToggle.tsx
│   │   │   ├── Clock.tsx
│   │   │   ├── PinDot.tsx
│   │   │   ├── PinPadButton.tsx
│   │   │   ├── StatusBadge.tsx
│   │   │   ├── TierBadge.tsx
│   │   │   ├── EmptyState.tsx
│   │   │   └── LoadingSpinner.tsx
│   │   ├── molecules/
│   │   │   ├── SearchInput.tsx
│   │   │   ├── MemberAvatar.tsx
│   │   │   ├── ActivityFeedItem.tsx
│   │   │   ├── ActiveMemberRow.tsx
│   │   │   ├── MemberSearchResult.tsx
│   │   │   ├── StatCard.tsx
│   │   │   ├── KPICard.tsx
│   │   │   └── CapacityBar.tsx
│   │   ├── organisms/
│   │   │   ├── TopBar.tsx
│   │   │   ├── PoolCountCard.tsx
│   │   │   ├── CurrentlyInPoolCard.tsx
│   │   │   ├── RecentActivityCard.tsx
│   │   │   ├── NewMembersCard.tsx
│   │   │   ├── PinPad.tsx
│   │   │   ├── MemberDetailSheet.tsx
│   │   │   └── ManagerAuthGate.tsx
│   │   └── layout/
│   │       ├── AppShell.tsx
│   │       └── AuthLayout.tsx
│   ├── routes/
│   │   ├── __root.tsx          ← root layout
│   │   ├── login.tsx
│   │   ├── _authenticated.tsx  ← layout that wraps protected routes
│   │   ├── _authenticated/
│   │   │   ├── dashboard.tsx
│   │   │   ├── members.tsx
│   │   │   ├── members.$id.tsx
│   │   │   └── reports.tsx
│   │   └── 404.tsx
│   ├── hooks/
│   │   ├── useDashboardSummary.ts
│   │   ├── useActivePool.ts
│   │   ├── useRecentActivity.ts
│   │   ├── useMemberSearch.ts
│   │   ├── useMember.ts
│   │   ├── useSignOutMember.ts
│   │   ├── useCheckInMember.ts
│   │   ├── useLogin.ts
│   │   ├── useTheme.ts
│   │   └── useConnectionStatus.ts
│   └── test/
│       └── setup.ts

═══════════════════════════════════════════════════════════════
DESIGN TOKENS — SOURCE OF TRUTH
═══════════════════════════════════════════════════════════════

Copy these EXACTLY into src/styles/globals.css and tailwind.config.ts.
(Full versions are in Design-Tokens.md in the repo root.)

CSS VARIABLES (globals.css):
:root {
  /* Primitives (raw colors) */
  --teal-50:#E3F2FD; --teal-100:#BBDEFB; --teal-200:#90CAF9;
  --teal-300:#64B5F6; --teal-400:#42A5F5; --teal-500:#2196F3;
  --teal-600:#1E88E5; --teal-700:#1976D2; --teal-800:#1565C0; --teal-900:#0D47A1;
  --navy-50:#E8EAF0; --navy-100:#C5CAD9; --navy-200:#9DA7BD;
  --navy-300:#7584A1; --navy-400:#58698D; --navy-500:#3B4F79;
  --navy-600:#354871; --navy-700:#1B3A5C; --navy-800:#15294A; --navy-900:#0F1B2D;
  --gray-50:#FAFAFA; --gray-100:#F5F5F5; --gray-200:#EEEEEE;
  --gray-300:#E0E0E0; --gray-400:#BDBDBD; --gray-500:#9E9E9E;
  --gray-600:#757575; --gray-700:#616161; --gray-800:#424242; --gray-900:#212121;
  --red-50:#FEE2E2; --red-500:#D32F2F; --red-700:#B71C1C;
  --green-50:#E8F5E9; --green-500:#2E7D32; --green-700:#1B5E20;
  --yellow-50:#FFF8E1; --yellow-500:#F9A825; --yellow-700:#F57F17;
  --aqua-50:#F0F7FA;
  --white:#FFFFFF; --black:#000000;

  /* Spacing */
  --spacing-xs:4px; --spacing-sm:8px; --spacing-md:16px;
  --spacing-lg:24px; --spacing-xl:32px; --spacing-2xl:48px; --spacing-3xl:64px;

  /* Radii */
  --radius-sm:4px; --radius-md:8px; --radius-lg:12px;
  --radius-xl:16px; --radius-2xl:20px; --radius-full:9999px;

  /* Shadows */
  --shadow-sm:0 1px 2px rgba(27,58,92,0.06);
  --shadow-md:0 4px 12px rgba(27,58,92,0.08);
  --shadow-lg:0 8px 24px -4px rgba(27,58,92,0.12);
  --shadow-xl:0 16px 40px -8px rgba(27,58,92,0.16);
}

:root, [data-theme="light"] {
  --bg-canvas: var(--aqua-50);
  --bg-surface: var(--white);
  --bg-surface-raised: var(--white);
  --bg-muted: var(--gray-100);
  --bg-hover: var(--gray-50);
  --text-primary: var(--navy-900);
  --text-secondary: var(--gray-700);
  --text-tertiary: var(--gray-500);
  --text-inverse: var(--white);
  --text-accent: var(--teal-700);
  --border-default: var(--gray-200);
  --border-strong: var(--gray-300);
  --border-focus: var(--teal-500);
  --border-accent: var(--teal-500);
  --accent-primary: var(--teal-500);
  --accent-primary-hover: var(--teal-600);
  --accent-primary-text: var(--white);
  --success-bg: var(--green-50);
  --success-text: var(--green-700);
  --success-icon: var(--green-500);
  --warning-bg: var(--yellow-50);
  --warning-text: var(--yellow-700);
  --warning-icon: var(--yellow-500);
  --danger-bg: var(--red-50);
  --danger-text: var(--red-700);
  --danger-icon: var(--red-500);
}

[data-theme="dark"] {
  --bg-canvas: var(--navy-900);
  --bg-surface: var(--navy-800);
  --bg-surface-raised: var(--navy-700);
  --bg-muted: var(--navy-800);
  --bg-hover: var(--navy-700);
  --text-primary: var(--white);
  --text-secondary: var(--gray-300);
  --text-tertiary: var(--gray-400);
  --text-inverse: var(--navy-900);
  --text-accent: var(--teal-300);
  --border-default: var(--navy-700);
  --border-strong: var(--navy-600);
  --border-focus: var(--teal-400);
  --border-accent: var(--teal-400);
  --accent-primary: var(--teal-400);
  --accent-primary-hover: var(--teal-300);
  --accent-primary-text: var(--white);
  --success-bg: var(--green-700);
  --success-text: var(--green-500);
  --success-icon: var(--green-500);
  --warning-bg: var(--yellow-700);
  --warning-text: var(--yellow-500);
  --warning-icon: var(--yellow-500);
  --danger-bg: var(--red-700);
  --danger-text: var(--red-500);
  --danger-icon: var(--red-500);
}

* { -webkit-tap-highlight-color: transparent; }
body {
  background: var(--bg-canvas);
  color: var(--text-primary);
  font-family: 'Inter', -apple-system, system-ui, sans-serif;
  font-feature-settings: 'cv11', 'ss01';
  -webkit-font-smoothing: antialiased;
  transition: background 200ms ease, color 200ms ease;
}

TAILWIND CONFIG (tailwind.config.ts): use the full version from 
Design-Tokens.md — it maps every CSS variable to a Tailwind class.

═══════════════════════════════════════════════════════════════
SHADCN COMPONENTS TO INSTALL
═══════════════════════════════════════════════════════════════

Run these (one per line):
  pnpm dlx shadcn@latest add button
  pnpm dlx shadcn@latest add card
  pnpm dlx shadcn@latest add input
  pnpm dlx shadcn@latest add label
  pnpm dlx shadcn@latest add sheet
  pnpm dlx shadcn@latest add dialog
  pnpm dlx shadcn@latest add badge
  pnpm dlx shadcn@latest add avatar
  pnpm dlx shadcn@latest add tabs
  pnpm dlx shadcn@latest add table
  pnpm dlx shadcn@latest add separator
  pnpm dlx shadcn@latest add tooltip
  pnpm dlx shadcn@latest add scroll-area
  pnpm dlx shadcn@latest add skeleton
  pnpm dlx shadcn@latest add dropdown-menu
  pnpm dlx shadcn@latest add alert
  pnpm dlx shadcn@latest add progress

After install, customize each shadcn primitive to use OUR tokens, not 
slate. Example: in button.tsx, change `bg-primary` to `bg-accent`, 
`hover:bg-primary/90` to `hover:bg-accent-hover`. Map every shadcn 
color reference to our semantic tokens.

═══════════════════════════════════════════════════════════════
API CLIENT (src/lib/api.ts)
═══════════════════════════════════════════════════════════════

import axios from 'axios';
import { getToken } from './auth';

export const api = axios.create({
  baseURL: '/api/v1',
  timeout: 10000,
});

api.interceptors.request.use((config) => {
  const token = getToken();
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  (err) => {
    if (err.response?.status === 401) {
      // Clear auth, route to login
      window.dispatchEvent(new Event('auth:unauthorized'));
    }
    return Promise.reject(err);
  }
);

═══════════════════════════════════════════════════════════════
AUTH (src/lib/auth.ts) — In-memory only, no localStorage
═══════════════════════════════════════════════════════════════

let _token: string | null = null;
let _user: { id: string; name: string; role: 'lifeguard'|'manager'|'admin' } | null = null;

export function setAuth(token: string, user: typeof _user) {
  _token = token;
  _user = user;
}
export function getToken() { return _token; }
export function getUser() { return _user; }
export function clearAuth() { _token = null; _user = null; }
export function isAuthenticated() { return _token !== null; }
export function hasRole(role: string) { return _user?.role === role || _user?.role === 'admin'; }

═══════════════════════════════════════════════════════════════
PAGE 1 — LOGIN (/login)
═══════════════════════════════════════════════════════════════

Layout:
  - Full viewport, bg-canvas
  - Subtle water-ripple SVG background pattern, very low opacity (~5%)
  - Centered card (max-w-md):
    - Wedgewood logo (48px height) — img src:
      https://assets.cdn.filesafe.space/Bjt6c984XN3YKY5porzI/media/6980bb3566e7ca30baf9488c.png
    - H1: "Welcome to Wedgewood Pool"
    - Caption: "Enter your staff PIN to continue"
    - 4-dot PIN indicator (filled dots = entered digits)
    - 4×3 number pad grid (1-9, blank, 0, backspace icon)
    - Buttons are 72px × 72px, rounded-2xl, bg-surface, shadow-sm
    - On 4th digit entered, auto-submit via useLogin hook
    - Loading state: replace dots with spinner
    - Error state: red border on dots, shake animation, clear after 600ms
  - Footer: small "Powered by Venderly" text, muted

Behavior:
  - PIN stored in component state (never persisted)
  - On success: setAuth() + navigate to /dashboard
  - On 401: shake, clear, focus first dot
  - Theme toggle in top-right corner
  - Pressing physical keyboard digits works too (for laptop testing)

═══════════════════════════════════════════════════════════════
PAGE 2 — DASHBOARD (/dashboard)
═══════════════════════════════════════════════════════════════

Layout (iPad landscape 1366×1024 primary):

[TopBar: 64px sticky]
  Left: Logo + "Wedgewood Pool"
  Center: empty or breadcrumb
  Right: Clock · ConnectionIndicator · ThemeToggle · UserMenu (avatar)

[Hero Section — PoolCountCard, full width, 200px tall]
  Two-column layout:
  Left (60%): 
    Caption: "Currently in the pool"
    Big number (Number XL, 56px tabular): "{currentlyInPool}"
    Small text: "swimmers"
  Right (40%):
    Caption: "Pool capacity"
    CapacityBar: 
      Horizontal bar, full width of right column
      Filled portion = currentlyInPool / poolCapacity
      Color: 0-70% green, 70-90% yellow, 90-100% red, >100% pulsing red
      Below bar: "{currentlyInPool} / {poolCapacity}"
    If at/over capacity: red banner above bar "⚠️ At capacity"

[Search Bar — full width, 56px tall, between hero and grid]
  Large input with search icon (lucide Search)
  Placeholder: "Search by name, phone, or email…"
  Debounced 300ms
  Results drop below as a sheet (z-index above grid)

[3-Column Grid — fills remaining viewport]
  Equal width columns, gap-lg

  Column 1: CurrentlyInPoolCard (1fr)
    Header: "In Pool Now" + count badge
    Reset Pool button (icon-only, gray, top-right)
    Body: scroll-area with ActiveMemberRow list
      - Avatar (initials)
      - Name (Body Medium)
      - "Since {time}" (Body Small, secondary)
      - "Sign Out" button (right-aligned, sm, ghost variant)
    Empty: friendly empty state "🌊 Pool is quiet right now"

  Column 2: RecentActivityCard (1fr)
    Header: "Recent Activity"
    Body: scroll-area with ActivityFeedItem list (10 most recent)
      - Icon based on type:
        check_in: lucide LogIn, success/icon color
        sign_out: lucide LogOut, text-accent color
        signup: lucide UserPlus, accent color
        manual_checkin: lucide UserCheck, accent color
        manual_signout: lucide UserMinus, text-secondary
      - Name (Body Medium)
      - Verb based on type ("Checked in", "Signed out", etc.)
      - Time relative ("just now", "5 min ago", "2:48 PM")
      - Subtle row hover

  Column 3: NewMembersCard (1fr)
    Header: "New Members Today"
    Big number (Number L, 32px): "{newMembersToday}"
    Below: list of avatars (stacked, max 5 visible + "+N more")
    On click: opens sheet with full list

[Footer: 40px, bottom]
  Last polled time: "Updated {time}" (caption, tertiary)
  Right: small links — "Members" / "Reports" / "Help"

Real-time behavior:
  - useQuery({ queryKey: ['summary'], refetchInterval: 3000 })
  - useQuery({ queryKey: ['active'], refetchInterval: 3000 })
  - useQuery({ queryKey: ['recent'], refetchInterval: 3000 })
  - useSSE() hook adds events to recent feed instantly
  - If SSE drops: indicator turns yellow, polling continues
  - If polling fails 3x: indicator turns red, dim cards to 70% opacity
  - On new check-in event: brief teal pulse on PoolCountCard + soft Web Audio chime
  - prefers-reduced-motion: respect, no pulses

Manual sign-out flow:
  - Click "Sign Out" on ActiveMemberRow
  - native confirm("Sign out {name}? They might still be at the pool.")
  - On confirm: POST /dashboard/signout/manual
  - On success: optimistic remove from active list + toast "Signed out {name}"
  - On failure: toast.error

Search flow:
  - Type 2+ chars
  - 300ms debounce
  - Results panel slides down below search bar
  - Each result: avatar + name + tier badge + status + "View" button
  - Tap result → opens MemberDetailSheet (slide-over from right)

═══════════════════════════════════════════════════════════════
PAGE 3 — MEMBERS (/members)
═══════════════════════════════════════════════════════════════

Layout:
[TopBar — same as dashboard]

[Page Header — 80px]
  H1: "Members"
  Subtext: "{total} members · {active} active"
  Right: "Add Member" button (primary)

[Filter Bar — 56px sticky below header]
  Large search input (full width on left, 60%)
  Tier filter pills: All / Family / Adult / Student
  Status filter: All / Active / Expired
  Sort dropdown: Name A-Z / Recent / Tier

[Members Table]
  shadcn Table with these columns:
    Avatar (40px) | Name | Tier | Family Count | Status | Last Visit | Guest Passes | Actions
  Row hover: bg-hover
  Tap row: opens MemberDetailSheet
  Sticky header
  Empty state: "No members match your search"
  Virtualized scroll for >100 members (use @tanstack/react-virtual)

MemberDetailSheet (slide-over from right, 480px wide):
  Top:
    Close button (X) top-right
    Avatar (96px) + name (H1) + tier badge
    Status badge (active/expired)
  Tabs: Overview | Family | History | Notes

  Overview tab:
    Contact: phone (tap-to-call), email (tap-to-email)
    Address (if available)
    Member since: date
    Membership: start - end dates
    Guest passes: "{remaining} / {total} remaining" + Buy More button
    Payment: tier amount + status

  Family tab:
    List of family member rows
    Each row: avatar, name, age, relationship, individual check-in button

  History tab:
    Timeline of last 30 check-ins
    Format: "Checked in 2:48 PM · Signed out 4:12 PM (1h 24m)"

  Notes tab:
    Read-only field showing emergency contact, allergies
    Edit button (manager+ only)

  Action bar at bottom:
    [Check In] [Sign Out] [Edit (manager+)] [Delete (admin)]

═══════════════════════════════════════════════════════════════
PAGE 4 — REPORTS (/reports) — Manager only
═══════════════════════════════════════════════════════════════

Auth gate: ManagerAuthGate wraps. If user.role !== manager/admin, 
show "Reports require manager access" with link back to dashboard.

Layout:
[TopBar]

[Page Header]
  H1: "Reports"
  Time range selector dropdown: Today / Yesterday / Last 7 days / This month / Custom
  Export button (downloads CSV)

[KPI Grid — 4 cards in a row]
  Each card 1fr, 140px tall:
  
  Card 1 — Visits
    Caption: "Visits"
    Number L: total visits in range
    Below: green/red delta vs previous period
  
  Card 2 — Guests
    Caption: "Guests"
    Number L: guest checkins
    Below: "${revenue}" guest revenue
  
  Card 3 — New Signups
    Caption: "New Members"
    Number L: signups in range
    Below: "{free_gifts} free gifts" if any
  
  Card 4 — Active Members
    Caption: "Active Members"
    Number L: total active memberships
    Below: "% capacity" used

[Charts Section — 2-column grid]
  Chart 1: Attendance Trend (line chart, last 30 days)
    X: days, Y: visits
    Line: teal/500
    Use Recharts (install: pnpm install recharts)
  
  Chart 2: Hourly Distribution (bar chart, today)
    X: hours of day, Y: visits
    Bars: teal/500 with hover state

[Guest Pass Inventory Table]
  Columns: Member | Total Bought | Used | Remaining | Last Purchase
  Sort by Remaining ascending (low passes first — sales opportunity)

═══════════════════════════════════════════════════════════════
TOP BAR (shared component)
═══════════════════════════════════════════════════════════════

Sticky top, bg-surface, border-b border-default, 64px tall, px-lg
flex: items-center, gap-lg

Left:
  Logo (32px height)
  Vertical separator (8px tall, border-default)
  "Wedgewood Pool" — H3, hidden on mobile <768px

Center: flexgrow, breadcrumb if not on dashboard
  ChevronRight separators between segments
  Last segment: bold

Right (gap-md):
  Live Clock (caption, tabular-nums) — useClock hook updates every second
  ConnectionIndicator dot (green/yellow/red, with tooltip on hover)
  ThemeToggle (sun/moon icon)
  UserMenu (avatar dropdown):
    - User name + role
    - Settings link
    - Sign out button

═══════════════════════════════════════════════════════════════
CONNECTION INDICATOR LOGIC
═══════════════════════════════════════════════════════════════

const useConnectionStatus = () => {
  const [state, setState] = useState<'live'|'reconnecting'|'offline'>('live');
  const lastPollSuccess = useRef(Date.now());
  
  // Subscribe to query events
  // After 1 failed poll: 'reconnecting'
  // After 3 failed polls in a row: 'offline'
  // On any success: 'live'
  
  return { state, lastSuccess: lastPollSuccess.current };
};

Visual:
  live: green dot, "Live"
  reconnecting: yellow dot, "Reconnecting…"
  offline: red dot pulsing, "Offline — last updated {time}"
  
When offline: dim all cards on dashboard to opacity-70 + show 
"Last updated {time}" caption under each card title.

═══════════════════════════════════════════════════════════════
THEME TOGGLE
═══════════════════════════════════════════════════════════════

useTheme() hook (src/lib/theme.ts):
  - State: 'light' | 'dark' | 'system'
  - Default: 'system' (reads prefers-color-scheme)
  - Toggle cycles: system → light → dark → system
  - Applies via document.documentElement.setAttribute('data-theme', resolved)
  - No localStorage (per app constraints — use Zustand in-memory store)

═══════════════════════════════════════════════════════════════
ACCESSIBILITY REQUIREMENTS
═══════════════════════════════════════════════════════════════

- All interactive elements: min 44px × 44px touch target
- Color contrast: 4.5:1 minimum (WCAG AA)
- All buttons have aria-label or visible text
- Forms use shadcn Label, errors linked via aria-describedby
- Focus visible on all interactive elements (ring-2 ring-border-focus)
- Skip-to-content link at top
- prefers-reduced-motion respected (disable pulses, transitions)
- Tested with axe-core in dev (npm script: pnpm test:a11y)

═══════════════════════════════════════════════════════════════
IPAD SAFARI OPTIMIZATIONS
═══════════════════════════════════════════════════════════════

In index.html:
  <meta name="viewport" content="width=device-width, initial-scale=1, 
        maximum-scale=1, user-scalable=no, viewport-fit=cover">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="theme-color" content="#2196F3">

In CSS:
  - All inputs font-size: 16px+ (prevents auto-zoom on iOS)
  - Body: overscroll-behavior: none
  - All buttons: touch-action: manipulation
  - Request Wake Lock API on dashboard mount (keeps iPad awake)

═══════════════════════════════════════════════════════════════
INTEGRATION WITH EXPRESS BACKEND
═══════════════════════════════════════════════════════════════

The Express server (root project) needs to serve the React build:

// In src/server.ts, AFTER all API routes but BEFORE 404 handler:
app.use(express.static(path.join(__dirname, '../frontend/dist')));
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '../frontend/dist/index.html'));
});

Add to root package.json scripts:
  "frontend:dev": "cd frontend && pnpm dev",
  "frontend:build": "cd frontend && pnpm build",
  "build": "pnpm frontend:build && tsc",
  "dev": "concurrently \"pnpm dev:backend\" \"pnpm frontend:dev\""

Vite proxy config (frontend/vite.config.ts):
  server: {
    proxy: {
      '/api': 'http://localhost:3000',
      '/sse': { target: 'http://localhost:3000', ws: false, changeOrigin: true },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
  },

═══════════════════════════════════════════════════════════════
TESTING
═══════════════════════════════════════════════════════════════

Unit tests (Vitest):
  - All atoms tested for rendering + props
  - All hooks tested with mocked react-query
  - Coverage target: 70%+

E2E (Playwright):
  - Login flow (PIN entry → dashboard loads)
  - Manual sign-out (search → tap row → sheet → sign out → toast)
  - Theme toggle persists during session
  - Reports route gated by role

Run before committing:
  pnpm typecheck
  pnpm lint
  pnpm test
  pnpm test:e2e

═══════════════════════════════════════════════════════════════
COMMIT STRATEGY
═══════════════════════════════════════════════════════════════

Build in this order with separate commits per step:

1. "scaffold: Vite + React + TS + Tailwind in frontend/"
2. "tokens: apply Design-Tokens.md to Tailwind + globals.css"
3. "shadcn: install + customize primitives with our tokens"
4. "api: axios client + auth + TanStack Query setup"
5. "routes: TanStack Router with login + dashboard + members + reports"
6. "atoms: Logo, Clock, ConnectionIndicator, ThemeToggle, PinDot, badges"
7. "molecules: SearchInput, MemberAvatar, ActivityFeedItem, ActiveMemberRow, CapacityBar, StatCard"
8. "organisms: TopBar, PoolCountCard, CurrentlyInPoolCard, RecentActivityCard"
9. "page: Login with PIN pad"
10. "page: Dashboard with real-time polling + SSE"
11. "page: Members with detail sheet"
12. "page: Reports with charts"
13. "express: serve React build from src/server.ts"
14. "ipad: viewport + wake lock + audio chime"
15. "tests: unit tests for atoms + hooks + E2E for critical paths"

Push after each commit. Each commit must pass typecheck + lint + tests.

═══════════════════════════════════════════════════════════════
PROCEED
═══════════════════════════════════════════════════════════════

Start with commit 1. Confirm each step works (typecheck, dev server 
starts, page renders) before moving to next. Take screenshots after 
each page is built so you can verify visual output. If you're unsure 
about a styling decision, default to what Linear or Vercel's 
dashboard would do — clean, lots of whitespace, subtle borders, 
no decorative noise.

When done, push to main and tell me the deploy URL.
```

---

After Codex finishes scaffolding the React app, hand it the SECOND prompt for the member-facing pages (`Codex-Member-Pages-Prompt.md`).
